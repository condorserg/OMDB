<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MaterialDesign" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/deep_orange</item>
        <item name="colorPrimaryVariant">@color/orange</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/deep_brown</item>
        <item name="colorSecondaryVariant">@color/brown</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
    <!-- Bottom Navigation -->
    <style name="BottomNavigationView" parent="Widget.Design.BottomNavigationView">
        <item name="backgroundTint">@color/blue_grey</item>
        <item name="background">?android:attr/windowBackground</item>
        <item name="labelVisibilityMode">selected</item>
    </style>
    <style name="ItemMovieTitleText" parent="TextAppearance.Material3.HeadlineMedium">
        <item name="android:textAppearance">@style/TextAppearance.MaterialComponents.Headline6</item>
        <item name="android:textSize">@dimen/text_size_title</item>
        <item name="android:fontFamily">"sans-serif-medium"</item>
    </style>
    <style name="ItemMovieText" parent="TextAppearance.AppCompat.Small">
        <item name="android:textSize">@dimen/text_size_secondary</item>
        <item name="android:textColor">@color/black</item>
    </style>
</resources>